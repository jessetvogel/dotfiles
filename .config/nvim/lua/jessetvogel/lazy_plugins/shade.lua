return {
    --    {
    --        "sunjon/Shade.nvim",
    --        lazy = false, -- make sure we load this during startup if it is your main colorscheme
    --        event = "VeryLazy",
    --        config = function()
    --            require("shade").setup({
    --                overlay_opacity = 60,
    --            })
    --        end,
    --    },
}
